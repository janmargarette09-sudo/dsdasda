// assets/js/match_engine.js — Trigger auto-match, show progress bar

document.addEventListener('DOMContentLoaded', () => {
    const btn = document.getElementById('run-match-btn');
    if (!btn) return;
});
